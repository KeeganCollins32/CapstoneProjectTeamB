$(document).ready(function () {
    $('.hero-slider').slick({
        autoplay: true,
        autoplaySpeed: 3000,
        dots: true, // Enable dots navigation
        arrows: false, // Disable arrow navigation
        fade: true,
        cssEase: 'linear'
    });
});